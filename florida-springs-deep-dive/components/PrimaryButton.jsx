import React from 'react'
import { Button } from './ui/button'

const PrimaryButton = () => {
  return (
    <div>
        <Button className = 'bg-theme btn btn-primary'>Sign up</Button>
    </div>
  )
}

export default PrimaryButton